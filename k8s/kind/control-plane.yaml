apiVersion: apps/v1
kind: Deployment
metadata:
  name: duckgres-control-plane
  namespace: duckgres
  labels:
    app: duckgres-control-plane
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: duckgres-control-plane
  template:
    metadata:
      labels:
        app: duckgres-control-plane
    spec:
      terminationGracePeriodSeconds: 900
      serviceAccountName: duckgres-control-plane
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: control-plane
          image: duckgres:test
          imagePullPolicy: IfNotPresent
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_UID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.uid
          args:
            - "--mode"
            - "control-plane"
            - "--worker-backend"
            - "remote"
            - "--config-store"
            - "postgres://duckgres:duckgres@duckgres-config-store:5432/duckgres_config?sslmode=disable"
            - "--config-poll-interval"
            - "2s"
            - "--handover-drain-timeout"
            - "15m"
            - "--k8s-worker-image"
            - "duckgres:test"
            - "--k8s-worker-image-pull-policy"
            - "IfNotPresent"
            - "--k8s-worker-service-account"
            - "duckgres-worker"
            - "--k8s-worker-secret"
            - "duckgres-worker-token"
            - "--k8s-worker-configmap"
            - "duckgres-config"
            - "--k8s-shared-warm-target"
            - "1"
            - "--cert"
            - "/certs/server.crt"
            - "--key"
            - "/certs/server.key"
            - "--flight-port"
            - "8815"
            - "--config"
            - "/etc/duckgres/duckgres.yaml"
          ports:
            - name: pg
              containerPort: 5432
              protocol: TCP
            - name: flight
              containerPort: 8815
              protocol: TCP
            - name: api
              containerPort: 8080
              protocol: TCP
          volumeMounts:
            - name: config
              mountPath: /etc/duckgres
              readOnly: true
            - name: certs
              mountPath: /certs
            - name: data
              mountPath: /data
          readinessProbe:
            httpGet:
              path: /health
              port: api
            initialDelaySeconds: 2
            periodSeconds: 2
            failureThreshold: 15
          securityContext:
            allowPrivilegeEscalation: false
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
      volumes:
        - name: config
          configMap:
            name: duckgres-config
        - name: certs
          emptyDir: {}
        - name: data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: duckgres
  namespace: duckgres
  labels:
    app: duckgres-control-plane
spec:
  type: ClusterIP
  ports:
    - name: pg
      port: 5432
      targetPort: pg
      protocol: TCP
    - name: flight
      port: 8815
      targetPort: flight
      protocol: TCP
    - name: api
      port: 8080
      targetPort: api
      protocol: TCP
  selector:
    app: duckgres-control-plane
